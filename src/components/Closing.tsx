import React from 'react';

export default function Closing() {
  return (
    <footer className="py-24 px-6 border-t border-gray-100 mt-12 bg-gray-50/50">
      <div className="max-w-3xl mx-auto text-center">
        <p className="text-xl text-gray-600 leading-relaxed">
          If this is useful, I’d be happy to walk through how this could fit into a real support workflow.
        </p>
        <div className="mt-12 text-sm text-gray-400">
          Signal Layer — Internal Concept
        </div>
      </div>
    </footer>
  );
}
